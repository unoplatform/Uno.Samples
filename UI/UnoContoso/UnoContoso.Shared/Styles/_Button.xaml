<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="EllipsisButtonStyle" TargetType="Button" BasedOn="{StaticResource EllipsisButton}">
        <Setter Property="Background" Value="{ThemeResource SystemControlTransparentBrush}" />
        <Setter Property="Foreground" Value="{ThemeResource SystemControlForegroundBaseHighBrush}" />
        <Setter Property="BorderBrush" Value="{ThemeResource SystemControlTransparentBrush}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Width" Value="40" />
        <Setter Property="Height" Value="40" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>
    <Style x:Key="BigEllipsisButtonStyle" TargetType="Button" 
           BasedOn="{StaticResource EllipsisButtonStyle}">
        <Setter Property="Padding" Value="5" />
        <Setter Property="Width" Value="50" />
        <Setter Property="Height" Value="50" />
    </Style>
    <Style x:Key="DefaultTextButtonStyle" TargetType="Button" 
           BasedOn="{StaticResource EllipsisButtonStyle}">
    </Style>

</ResourceDictionary>
