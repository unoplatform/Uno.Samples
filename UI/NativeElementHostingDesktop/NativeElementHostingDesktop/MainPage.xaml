<Page x:Class="NativeElementHostingDesktop.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:NativeElementHostingDesktop"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
  <ScrollViewer IsTabStop="True">
    <Grid>
      <StackPanel
        HorizontalAlignment="Center"
        VerticalAlignment="Center">
        <TextBlock AutomationProperties.AutomationId="HelloTextBlock"
          Text="You should see an Xterm terminal window on Linux and a Powershell window on Windows."
          HorizontalAlignment="Center" />
        <ContentPresenter Width="300" Height="300" x:Name="cp" />
      </StackPanel>
    </Grid>
  </ScrollViewer>
</Page>
