<Page x:Class="ScrollAnchoring.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:ScrollAnchoring"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
  <Grid Padding="16"
        RowDefinitions="Auto,Auto,Auto,*,Auto"
        RowSpacing="12">
    <StackPanel Grid.Row="0" Spacing="4">
      <TextBlock Text="Scroll Anchoring"
                 Style="{StaticResource TitleTextBlockStyle}" />
      <TextBlock Text="Insert or remove items above the anchored item. The ScrollViewer keeps the anchor's on-screen position stable instead of jumping."
                 TextWrapping="Wrap"
                 Style="{StaticResource BodyTextBlockStyle}" />
    </StackPanel>

    <StackPanel Grid.Row="1"
                Orientation="Horizontal"
                Spacing="8">
      <Button x:Name="InsertAboveButton"
              Click="InsertAbove_Click"
              Content="Insert above" />
      <Button x:Name="RemoveAboveButton"
              Click="RemoveAbove_Click"
              Content="Remove top" />
      <Button x:Name="InsertBelowButton"
              Click="InsertBelow_Click"
              Content="Insert below" />
      <Button x:Name="ScrollTopButton"
              Click="ScrollTop_Click"
              Content="Top" />
      <Button x:Name="ScrollBottomButton"
              Click="ScrollBottom_Click"
              Content="Bottom" />
    </StackPanel>

    <StackPanel Grid.Row="2"
                Orientation="Horizontal"
                Spacing="12">
      <TextBlock VerticalAlignment="Center"
                 Text="VerticalAnchorRatio:" />
      <ComboBox x:Name="RatioCombo"
                SelectionChanged="Ratio_SelectionChanged">
        <ComboBoxItem Content="NaN (disabled)" />
        <ComboBoxItem Content="0.0 (near edge)" />
        <ComboBoxItem Content="0.5 (center)"
                      IsSelected="True" />
        <ComboBoxItem Content="1.0 (far edge)" />
      </ComboBox>
      <TextBlock x:Name="CurrentAnchorText"
                 VerticalAlignment="Center"
                 Text="CurrentAnchor: (none)" />
    </StackPanel>

    <ScrollViewer x:Name="TargetScrollViewer"
                  Grid.Row="3"
                  Padding="0,0,12,0"
                  Background="{ThemeResource LayerFillColorDefaultBrush}"
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                  BorderThickness="1"
                  CornerRadius="8"
                  HorizontalScrollBarVisibility="Disabled"
                  HorizontalScrollMode="Disabled"
                  VerticalAnchorRatio="0.5"
                  VerticalScrollBarVisibility="Auto"
                  VerticalScrollMode="Enabled"
                  ViewChanged="TargetScrollViewer_ViewChanged">
      <StackPanel x:Name="ContentPanel"
                  Padding="8"
                  Orientation="Vertical" />
    </ScrollViewer>

    <TextBlock x:Name="OffsetText"
               Grid.Row="4"
               Style="{StaticResource CaptionTextBlockStyle}"
               Text="VerticalOffset: 0" />
  </Grid>
</Page>
