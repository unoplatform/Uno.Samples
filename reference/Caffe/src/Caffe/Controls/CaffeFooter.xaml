<UserControl
    x:Class="Caffe.Controls.CaffeFooter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:utu="using:Uno.Toolkit.UI">

    <utu:AutoLayout
        Orientation="Vertical"
        PrimaryAxisAlignment="Center"
        CounterAxisAlignment="Center"
        Padding="0,20">

        <!-- Accent Bar (reversed colors from header) -->
        <Grid Width="36" Height="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0"
                    Background="{StaticResource CaffeAccentRedBrush}"
                    CornerRadius="1.5,0,0,1.5" />
            <Border Grid.Column="1"
                    Background="{StaticResource CaffePrimaryBrush}"
                    CornerRadius="0,1.5,1.5,0" />
        </Grid>

    </utu:AutoLayout>

</UserControl>
