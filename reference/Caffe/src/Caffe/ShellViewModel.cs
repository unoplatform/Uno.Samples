namespace Caffe;

/// <summary>
/// Root navigation host VM. Caffe is a single-page tool, so the initial route
/// always lands on Main — there's no login gate or shell-level state.
/// </summary>
public class ShellViewModel
{
}
