namespace EnterpriseDashboard.Views;

// Empty VM paired with ShellPage so the navigation system can resolve the
// "Main" route. ShellPage's chrome interactions (theme toggle, range pills,
// refresh) stay in code-behind because they manipulate XAML resources directly.
public class ShellPageViewModel
{
}
