namespace Pens.Presentation;

/// <summary>
/// Host VM for the tab shell (MainPage). Exists so the tab routes can be
/// navigated to by ViewModel type (e.g. clear-back-stack login -> Main).
/// </summary>
public class MainViewModel
{
}
