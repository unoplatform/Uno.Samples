<hd:PreviewGroup x:Class="Voyago.Previews.TripsPreviews"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:local="using:Voyago.Presentation"
                 xmlns:mock="using:Voyago.Presentation.MockData"
                 xmlns:hd="using:Uno.UI.HotDesign"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 mc:Ignorable="d">

    <!-- The trip book with trips booked and history behind them, and for a brand-new traveller where
         both FeedViews sit on their empty state — a state the running app cannot reach.
         Each child supplies the design-time DataContext in XAML — the only safe place for the
         generated-ViewModel mock, which must never be seeded from a page constructor. No size on the
         <hd:Preview>: the surface fills the canvas / device frame. -->
    <hd:Preview PreviewName="Booked" Visibility="Collapsed">
        <local:TripsPage DataContext="{x:Bind mock:TripsPageMockData.Data}" />
    </hd:Preview>

    <hd:Preview PreviewName="First Trip" Visibility="Collapsed">
        <local:TripsPage DataContext="{x:Bind mock:TripsPageMockData.FirstTrip}" />
    </hd:Preview>

</hd:PreviewGroup>
