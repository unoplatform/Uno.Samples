using Uno.UI.HotDesign;

namespace Voyago.Previews;

// A preview group holding the FavoritesPage states (Saved / Nothing Saved) as named children.
public sealed partial class FavoritesPreviews : PreviewGroup
{
    public FavoritesPreviews() => this.InitializeComponent();
}
