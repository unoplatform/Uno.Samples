using Uno.UI.HotDesign;

namespace Voyago.Previews;

// A preview group holding the TripsPage states (Booked / First Trip) as named children.
public sealed partial class TripsPreviews : PreviewGroup
{
    public TripsPreviews() => this.InitializeComponent();
}
