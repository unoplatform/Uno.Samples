using Uno.UI.HotDesign;

namespace Voyago.Previews;

// A preview group holding the DestinationDetailDialog states (Not Booked / Booked) as named children.
public sealed partial class DestinationDetailDialogPreviews : PreviewGroup
{
    public DestinationDetailDialogPreviews() => this.InitializeComponent();
}
