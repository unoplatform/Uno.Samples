using Uno.UI.HotDesign;

namespace Voyago.Previews;

// A preview group holding the SearchPage states (Popular / No Results) as named children.
public sealed partial class SearchPreviews : PreviewGroup
{
    public SearchPreviews() => this.InitializeComponent();
}
