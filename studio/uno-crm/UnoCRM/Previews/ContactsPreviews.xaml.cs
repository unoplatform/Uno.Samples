using Uno.UI.HotDesign;

namespace UnoCRM.Previews;

// A preview group holding the ContactsPage states (All / No Results) as named children.
public sealed partial class ContactsPreviews : PreviewGroup
{
    public ContactsPreviews() => this.InitializeComponent();
}
