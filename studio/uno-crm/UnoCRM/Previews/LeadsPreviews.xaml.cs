using Uno.UI.HotDesign;

namespace UnoCRM.Previews;

// A preview group holding the LeadsPage states (Analytics / No Open Leads) as named children.
public sealed partial class LeadsPreviews : PreviewGroup
{
    public LeadsPreviews() => this.InitializeComponent();
}
