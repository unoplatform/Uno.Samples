using Uno.UI.HotDesign;

namespace UnoCRM.Previews;

// A preview group holding the DashboardPage states (Busy Tenant / No Activity) as named children.
public sealed partial class DashboardPreviews : PreviewGroup
{
    public DashboardPreviews() => this.InitializeComponent();
}
