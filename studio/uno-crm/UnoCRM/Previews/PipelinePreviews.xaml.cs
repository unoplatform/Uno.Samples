using Uno.UI.HotDesign;

namespace UnoCRM.Previews;

// A preview group holding the PipelinePage states (Full Board / Empty Board) as named children.
public sealed partial class PipelinePreviews : PreviewGroup
{
    public PipelinePreviews() => this.InitializeComponent();
}
