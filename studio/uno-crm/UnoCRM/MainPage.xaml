<Page x:Class="UnoCRM.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      Background="{ThemeResource DashboardPageBackgroundBrush}">

  <!--
    Navigation shell. Owns the chrome once for the whole app: a NavigationView pane on
    wide layouts and a bottom TabBar on narrow ones. The content region below is left
    empty - Uno.Extensions Navigation injects the page for the active route (Dashboard /
    Pipeline / Leads / Contacts) into it, so the pages themselves carry no nav chrome.
  -->
  <Grid x:Name="RootShellGrid" uen:Region.Attached="True">

    <VisualStateManager.VisualStateGroups>
      <VisualStateGroup x:Name="AdaptiveStates">
        <!-- Narrow (default): bottom TabBar, NavigationView pane hidden -->
        <VisualState x:Name="NarrowState">
          <VisualState.StateTriggers>
            <!-- Explicit 0-width trigger so this state is selected at load even when the
                 page is injected by navigation (a no-trigger default isn't reliably applied). -->
            <AdaptiveTrigger MinWindowWidth="0" />
          </VisualState.StateTriggers>
          <VisualState.Setters>
            <Setter Target="BottomTabs.Visibility" Value="Visible" />
            <Setter Target="NavView.IsPaneToggleButtonVisible" Value="False" />
            <Setter Target="NavView.PaneDisplayMode" Value="LeftMinimal" />
            <Setter Target="NavView.IsPaneOpen" Value="False" />
          </VisualState.Setters>
        </VisualState>
        <!-- Wide (>=700px): NavigationView sidebar, no TabBar -->
        <VisualState x:Name="WideState">
          <VisualState.StateTriggers>
            <AdaptiveTrigger MinWindowWidth="700" />
          </VisualState.StateTriggers>
          <VisualState.Setters>
            <Setter Target="BottomTabs.Visibility" Value="Collapsed" />
            <Setter Target="NavView.IsPaneToggleButtonVisible" Value="True" />
            <Setter Target="NavView.IsPaneVisible" Value="True" />
            <Setter Target="NavView.PaneDisplayMode" Value="Auto" />
          </VisualState.Setters>
        </VisualState>
      </VisualStateGroup>
    </VisualStateManager.VisualStateGroups>

    <!-- NavigationView: sidebar on Wide, hidden pane on Narrow -->
    <NavigationView x:Name="NavView"
                    uen:Region.Attached="True"
                    IsSettingsVisible="False"
                    IsBackButtonVisible="Collapsed">
      <NavigationView.MenuItems>
        <NavigationViewItem uen:Region.Name="Dashboard" Content="Dashboard">
          <NavigationViewItem.Icon>
            <FontIcon Glyph="&#xE80F;" />
          </NavigationViewItem.Icon>
        </NavigationViewItem>
        <NavigationViewItem uen:Region.Name="Pipeline" Content="Pipeline">
          <NavigationViewItem.Icon>
            <FontIcon Glyph="&#xE8C8;" />
          </NavigationViewItem.Icon>
        </NavigationViewItem>
        <NavigationViewItem uen:Region.Name="Leads" Content="Leads">
          <NavigationViewItem.Icon>
            <FontIcon Glyph="&#xE716;" />
          </NavigationViewItem.Icon>
        </NavigationViewItem>
        <NavigationViewItem uen:Region.Name="Contacts" Content="Contacts">
          <NavigationViewItem.Icon>
            <FontIcon Glyph="&#xE77B;" />
          </NavigationViewItem.Icon>
        </NavigationViewItem>
      </NavigationView.MenuItems>

      <NavigationView.Content>
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>

          <!-- Shared content area - navigation framework injects the active page here -->
          <Grid Grid.Row="0"
                uen:Region.Attached="True"
                uen:Region.Navigator="Visibility" />

          <!-- Bottom TabBar: visible on Narrow only -->
          <utu:TabBar x:Name="BottomTabs"
                      Grid.Row="1"
                      uen:Region.Attached="True"
                      Style="{StaticResource BottomTabBarStyle}"
                      Visibility="Collapsed">
            <utu:TabBarItem uen:Region.Name="Dashboard" Content="Dashboard">
              <utu:TabBarItem.Icon>
                <FontIcon Glyph="&#xE80F;" />
              </utu:TabBarItem.Icon>
            </utu:TabBarItem>
            <utu:TabBarItem uen:Region.Name="Pipeline" Content="Pipeline">
              <utu:TabBarItem.Icon>
                <FontIcon Glyph="&#xE8C8;" />
              </utu:TabBarItem.Icon>
            </utu:TabBarItem>
            <utu:TabBarItem uen:Region.Name="Leads" Content="Leads">
              <utu:TabBarItem.Icon>
                <FontIcon Glyph="&#xE716;" />
              </utu:TabBarItem.Icon>
            </utu:TabBarItem>
            <utu:TabBarItem uen:Region.Name="Contacts" Content="Contacts">
              <utu:TabBarItem.Icon>
                <FontIcon Glyph="&#xE77B;" />
              </utu:TabBarItem.Icon>
            </utu:TabBarItem>
          </utu:TabBar>
        </Grid>
      </NavigationView.Content>
    </NavigationView>
  </Grid>
</Page>
