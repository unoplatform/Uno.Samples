namespace UnoCRM.Presentation;

/// <summary>
/// Backs <see cref="MainPage"/>, the navigation shell that owns the NavigationView / TabBar chrome.
/// It holds no state of its own — the content pages carry their own models — so it is a bare record.
/// </summary>
public partial record MainModel;
