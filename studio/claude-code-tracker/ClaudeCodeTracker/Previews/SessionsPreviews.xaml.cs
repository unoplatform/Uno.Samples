using Uno.UI.HotDesign;

namespace ClaudeCodeTracker.Previews;

// A preview group holding the SessionsPage states as named children.
public sealed partial class SessionsPreviews : PreviewGroup
{
    public SessionsPreviews() => this.InitializeComponent();
}
