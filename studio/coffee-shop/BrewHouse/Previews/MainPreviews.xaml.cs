using Uno.UI.HotDesign;

namespace BrewHouse.Previews;

// A preview group holding the MainPage (navigation shell) states (Cart Badge / No Badge).
public sealed partial class MainPreviews : PreviewGroup
{
    public MainPreviews() => this.InitializeComponent();
}
