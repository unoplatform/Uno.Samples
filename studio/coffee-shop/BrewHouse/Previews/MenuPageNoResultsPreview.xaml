<hd:Preview x:Class="BrewHouse.Previews.MenuPageNoResultsPreview"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:local="using:BrewHouse.Presentation"
            xmlns:hd="using:Uno.UI.HotDesign"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            mc:Ignorable="d"
            Width="390"
            Height="844">

    <!-- Same page, a different data state: an active search that matches nothing, so the empty
         "No matches found" panel shows. The state comes from LoadDataContext() in the code-behind. -->
    <local:MenuPage />
</hd:Preview>
