using Uno.UI.HotDesign;

namespace BrewHouse.Previews;

// A preview group holding the ProductDetailPage states (Today's Special / Standard) as named children.
public sealed partial class ProductDetailPreviews : PreviewGroup
{
    public ProductDetailPreviews() => this.InitializeComponent();
}
