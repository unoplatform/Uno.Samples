using Uno.UI.HotDesign;

namespace BrewHouse.Previews;

// A preview group holding the HomePage states (With Cart / Empty Cart) as named children.
public sealed partial class HomePreviews : PreviewGroup
{
    public HomePreviews() => this.InitializeComponent();
}
