using Uno.UI.HotDesign;

namespace BrewHouse.Previews;

// A preview group holding the MenuPage states (Full Catalog / No Results) as named children.
public sealed partial class MenuPreviews : PreviewGroup
{
    public MenuPreviews() => this.InitializeComponent();
}
