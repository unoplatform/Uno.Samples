using Uno.UI.HotDesign;

namespace BrewHouse.Previews;

// A preview group holding the OrdersPage states (History / Empty) as named children.
public sealed partial class OrdersPreviews : PreviewGroup
{
    public OrdersPreviews() => this.InitializeComponent();
}
