<hd:PreviewGroup x:Class="FitBeginnerApp.Previews.ProgressPreviews"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:local="using:FitBeginnerApp.Presentation"
                 xmlns:mock="using:FitBeginnerApp.Presentation.MockData"
                 xmlns:hd="using:Uno.UI.HotDesign"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 mc:Ignorable="d">

    <!-- ProgressPage for an established member, and on day one — both FeedViews on their empty state.
         Each child supplies the page's design-time DataContext in XAML — the only safe place for the
         generated-ViewModel mock, which must never be seeded from the page constructor. No size on
         the <hd:Preview>: a page fills the canvas / device frame. -->
    <hd:Preview PreviewName="Full History" Visibility="Collapsed">
        <local:ProgressPage DataContext="{x:Bind mock:ProgressPageMockData.Data}" />
    </hd:Preview>

    <hd:Preview PreviewName="Just Started" Visibility="Collapsed">
        <local:ProgressPage DataContext="{x:Bind mock:ProgressPageMockData.JustStarted}" />
    </hd:Preview>

</hd:PreviewGroup>
