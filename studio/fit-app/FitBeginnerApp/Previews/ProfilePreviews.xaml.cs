using Uno.UI.HotDesign;

namespace FitBeginnerApp.Previews;

// A preview group holding the ProfilePage states (Established / New Member) as named children.
public sealed partial class ProfilePreviews : PreviewGroup
{
    public ProfilePreviews() => this.InitializeComponent();
}
