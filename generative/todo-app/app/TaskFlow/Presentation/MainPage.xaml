<Page x:Class="TaskFlow.Presentation.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <utu:SafeArea Insets="VisibleBounds">
        <Grid uen:Region.Attached="True">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Content area -->
        <Grid Grid.Row="0"
              uen:Region.Attached="True"
              uen:Region.Navigator="Visibility" />

        <!-- Bottom TabBar -->
        <utu:TabBar Grid.Row="1"
                    uen:Region.Attached="True"
                    Style="{StaticResource BottomTabBarStyle}">
            <utu:TabBarItem uen:Region.Name="Dashboard"
                            x:Uid="DashboardTab"
                            utu:TabBarItemExtensions.OnClickBehaviors="BackNavigation">
                <utu:TabBarItem.Icon>
                    <FontIcon Glyph="&#xE80F;" />
                </utu:TabBarItem.Icon>
            </utu:TabBarItem>
            <utu:TabBarItem uen:Region.Name="Tasks"
                            x:Uid="TasksTab"
                            utu:TabBarItemExtensions.OnClickBehaviors="BackNavigation">
                <utu:TabBarItem.Icon>
                    <FontIcon Glyph="&#xE762;" />
                </utu:TabBarItem.Icon>
            </utu:TabBarItem>
            <utu:TabBarItem uen:Region.Name="Categories"
                            x:Uid="CategoriesTab"
                            utu:TabBarItemExtensions.OnClickBehaviors="BackNavigation">
                <utu:TabBarItem.Icon>
                    <FontIcon Glyph="&#xE8FD;" />
                </utu:TabBarItem.Icon>
            </utu:TabBarItem>
            <utu:TabBarItem uen:Region.Name="Completed"
                            x:Uid="CompletedTab"
                            utu:TabBarItemExtensions.OnClickBehaviors="BackNavigation">
                <utu:TabBarItem.Icon>
                    <FontIcon Glyph="&#xE930;" />
                </utu:TabBarItem.Icon>
            </utu:TabBarItem>
        </utu:TabBar>
    </Grid>
    </utu:SafeArea>
</Page>
